#include <vector>

using namespace std;

void generate(vector<int> array, vector<vector<int>> &result) {
    for (vector<int>::size_type i = 0; i < array.size(); ++i)

}

vector<vector<int>> getPermutations(vector<int> array) {

    vector<vector<int>> result;
    generate(array, result);

    return {};
}
